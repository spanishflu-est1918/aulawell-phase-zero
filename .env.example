# Copy to .env.local for local use, and add the same variables in your Vercel
# project settings (Settings → Environment Variables) for the live site.
# Until these are set, the booking calendar shows a friendly "almost ready"
# message and the rest of the site works normally.

# Cal.com (free account): Settings → Security → API keys → create key
CAL_API_KEY=

# Cal.com event type id: create a "Lesson" event type (60 min), the id is the
# number in its URL, e.g. app.cal.com/event-types/123456 → 123456
CAL_EVENT_TYPE_ID=

# Optional: event type id for online lessons with the associate tutor.
# Until this is set, the associate-online option shows a contact message.
CAL_EVENT_TYPE_ID_ASSOCIATE=

# Optional: event type id for in-person (presencial) lessons with the
# associate tutor. Until this is set, the in-person option on the booking
# page shows "arranged personally — contact us" instead of live slots.
CAL_EVENT_TYPE_ID_PRESENCIAL=

# Free 30-minute consultation event type (no payment step). Until this is
# set, /consultation shows a friendly "almost ready" message.
CAL_EVENT_TYPE_ID_CONSULTATION=

# Stripe (stripe.com → Developers → API keys → Secret key, sk_live_... or sk_test_...)
# Keep this server-side only. Never prefix it with NEXT_PUBLIC_.
STRIPE_SECRET_KEY=

# Stripe webhook signing secret: Developers → Webhooks → Add endpoint
#   URL: https://YOUR-DOMAIN/api/stripe-webhook
#   Event: checkout.session.completed
# Online booking remains disabled until both Stripe variables are configured.
STRIPE_WEBHOOK_SECRET=

# Protects the daily invoicing job (/api/cron/postpay-invoices) that emails
# Stripe invoices for completed post-pay lessons. Any long random string —
# generate one with: openssl rand -hex 32
# When set in Vercel, its cron scheduler sends it automatically.
CRON_SECRET=

# Airtable — Aulawell's operational source of truth (leads, packages, bookings).
# Personal Access Token: airtable.com/create/tokens with data.records:write
# (and data.records:read) granted on the Aulawell base. Server-side only —
# never prefix with NEXT_PUBLIC_. Until this is set, enquiries and bookings
# still work (owner is notified by email); they simply are not written to
# Airtable.
AIRTABLE_API_KEY=

# Optional: override the base id (defaults to the shared Aulawell base).
AIRTABLE_BASE_ID=

# Optional: override table names/ids if they differ from the defaults in
# lib/airtable.ts (Leads defaults to the shared table id; Packages/Lessons
# default to those names).
AIRTABLE_LEADS_TABLE=
AIRTABLE_PACKAGES_TABLE=
AIRTABLE_LESSONS_TABLE=
